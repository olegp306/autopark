const fetchAllBuses = () => {
  //
};

const addBus = bus => {
  //
};
const updateBus = bus => {
  //
};

const fetchAllDrivers = () => {
  //
};

const addDriver = driver => {
  //
};
const updateDriver = driver => {
  //
};

export default {
  fetchAllBuses,
  addBus,
  updateBus,
  fetchAllDrivers,
  addDriver,
  updateDriver
};
